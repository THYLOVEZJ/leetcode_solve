package com.leetcode.maxConsecutiveAnswers;

public class Solution {
    public int maxConsecutiveAnswers(String answerKey, int k) {
        char[] ans = answerKey.toCharArray();
        int fnum = 0;
        int tnum = 0;
        int low = 0;
        int fast = 0;
        while (low < ans.length) {
            while (ans[low] == ans[fast]) {

            }
        }
    }
}
